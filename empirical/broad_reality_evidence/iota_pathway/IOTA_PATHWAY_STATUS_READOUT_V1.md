# Mutualist-interference pathway: status readout v1

This readout records the state of the declared constituent-pathway meta-analysis at the time the
analysis machinery and its pre-registration were committed. It is regenerated by re-running the
command in §3 after any change to the effect table or the moderator coding table.

## 1. What is declared and executable

| Component | File | State |
|---|---|---|
| Compatibility stratum | `empirical/broad_reality_evidence/broad_meta_analysis_strata.csv` | `BP_chemical_pollinator_use_lrr_manipulation` declared |
| Theory bridge | `docs/IOTA_PATHWAY_EMPIRICAL_TARGET.md` | bridge assumption B1 stated |
| Extraction protocol | `IOTA_PATHWAY_EXTRACTION_PROTOCOL_v1.md` | pre-registered |
| Moderator registry | `iota_moderator_registry.csv` | 4 analyses declared |
| Moderator coding | `iota_moderator_coding.csv` | header only |
| Reading queue | `iota_reading_queue.csv` | 15 candidates, none retrieved |
| Pooled analysis | `trait_architecture/broad_meta_analysis.py` | implemented, tested |
| Moderator analysis | `trait_architecture/context_dependence.py` | implemented, tested |

## 2. Current result

```text
declared_analysis_count          4
executed_meta_regression_count   0
executed_subgroup_count          0
eligible effect rows             0
independent study clusters       0
```

Every declared analysis returns `insufficient_moderator_capacity` and the verdict
`not_evaluated`. This is the code refusing to estimate below its predeclared thresholds, not a
missing analysis step. No pooled effect, no moderator contrast, and no context-dependence
verdict exists for this pathway yet, and none may be reported.

The abstract-level direction map is unchanged and remains at its own evidence level: the
`B_to_pollination × chemical_barrier × pollinator_preference_or_foraging × manipulation` cell
holds three independent primary clusters, all coded negative from Crossref-deposited abstracts.
Three abstract-level direction codes are not a quantitative meta-analysis and do not satisfy the
project's empirical target.

## 3. Reproduction

```bash
python scripts/run_context_dependence.py \
  empirical/broad_reality_evidence/broad_effect_extractions.csv \
  empirical/broad_reality_evidence/iota_pathway/iota_moderator_coding.csv \
  empirical/broad_reality_evidence/broad_meta_analysis_strata.csv \
  empirical/broad_reality_evidence/iota_pathway/iota_moderator_registry.csv \
  artifacts/supplement/iota_pathway
```

## 4. The remaining blocker

The gap between this state and the project's empirical target is entirely in extraction: the
candidate studies must be read at full text and their treatment/comparator responses, dispersions,
sample sizes, concentrations, natural-range citations, assay contexts, and pollinator groups
recorded with locators.

That work was not possible in the session that committed this layer. Outbound network access was
refused by the environment's egress policy for every bibliographic and full-text service required
— Crossref, OpenAlex, Europe PMC, PubMed Central, Dryad, and publisher domains all returned a
policy denial at the proxy. Candidate identifiers in the reading queue were recorded from search
results and are flagged accordingly; they must be verified against the source before any number
is extracted.

No effect value has been entered from a search snippet, an abstract, or a recollection of the
literature, and none may be. The empty effect table is the accurate representation of what has
been verified.

## 5. Next action when retrieval is available

1. Verify each reading-queue identifier against the publisher or a bibliographic service and
   update `identifier_verification_status`.
2. Screen against the inclusion and exclusion criteria in the protocol; record every exclusion
   with its reason.
3. Extract oriented effects into `broad_effect_extractions.csv` with `source_locator` on every row.
4. Code the declared moderators into `iota_moderator_coding.csv` with an explicit `coding_basis`.
5. Re-run §3 and this readout; report whichever verdict the declared thresholds produce.
