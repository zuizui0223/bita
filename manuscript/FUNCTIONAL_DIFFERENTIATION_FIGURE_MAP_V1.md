# BITA functional differentiation figure map v1

## Figure 1 — From compromise to dimensional release

**A. Shared architecture.**

```text
function 1 ---\
               >--- z ---> compromise
function 2 ---/
```

Show the **theory-level** pure function optima:

```text
z_F1*
z_F2*
```

and the ideal theory-level shared optimum `z_C,theory*`.

**B. Differentiated architecture.**

```text
function 1 -> x
function 2 -> y.
```

**C. Theory benchmark.** Show:

```text
Delta_mod,theory
  = [a b / (a + b)] (z_F1* - z_F2*)^2 - K.
```

Label this explicitly as theory-level, not the default empirical `Delta_mod` estimator.

**D. Empirical optimum release.** Show the SCH state-specific reference:

```text
z_P* = argmax W10(z)
```

and `x*(y1)` moving closer to `z_P*` than `x*(y0)`.

Optional inset: if SCH independently identifies pure `z_F1*`, show a second stricter release lane toward that target.

Primary message: empirical dimensional release uses the directly identified SCH state reference by default; pure-function release is a stronger optional claim.

## Figure 2 — Preferential functional loading

Display the trait-to-function response matrix:

```text
          function 1   function 2
x            r11          r12
y            r21          r22.
```

Illustrate:

1. integrated / pleiotropic;
2. partially differentiated;
3. strongly modular.

Primary message: two traits are not automatically two modules.

## Figure 3 — Multi-level dimensional release plus local A x D hierarchy

**A. Multi-level release.** Plot:

```text
W(x | y0)
W(x | y1)
```

with:

```text
x0*
x1*
z_P* SCH reference.
```

Define:

```text
R_state = |x0* - z_P*| - |x1* - z_P*|.
```

Optional pure-function lane:

```text
R_pure = |x0* - z_F1*| - |x1* - z_F1*|
```

only when SCH independently identifies `z_F1*`.

**B. Local A x D outcome hierarchy.** Show `W00`, `W10`, `W01`, `W11` and derive:

```text
A0
A1
Delta_AD W.
```

Then display:

```text
Level 1 interaction relief
Level 2 constraint release
Level 3 strict reversal.
```

Primary message: local interaction relief and multi-level dimensional release are related but distinct estimands.

## Figure 4 — 16-cell mechanism allocation and residual coupling

Show:

```text
A x D x antagonist x pollinator.
```

Map:

```text
rho_delta   antagonist relief
iota_delta  pollinator interference
m0_delta    pollinator-independent baseline term
U_delta     unallocated residual until independent assay.
```

Highlight the `A x D x G x P` four-way term as a residual functional-coupling diagnostic.

Primary message: mechanism allocation explains the observed dimensional release; it is not inferred from the release surface alone.

## Figure 5 — Real-world architecture evidence and claim ladder

Arrange evidence on:

```text
D0 distinct coordinates
D1 preferential functional loading
D2 state-specific dimensional release
D2+ optional pure-function release
D3 mechanism-resolved differentiation
D4 stable developmental/ecological modularity
D5 historical modularization.
```

Evidence anchors:

- floral-scent component partitioning;
- conditional cue gating;
- route separation / overlapped / bypassable defence architectures;
- Kessler 2008 A x D-like reproductive surface;
- current complete-system gap at D3;
- no direct historical shared-z -> x,y reconstruction.

Primary message: contemporary functional separation is real, but state-specific release, pure-function release, full mechanism allocation, and historical modularization are progressively stronger claims.
