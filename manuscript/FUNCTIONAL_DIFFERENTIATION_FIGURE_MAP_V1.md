# BITA functional differentiation figure map v1

## Figure 1 — From compromise to dimensional release

**A. Shared architecture.**

```text
function 1 ---\
               >--- z ---> compromise
function 2 ---/
```

Show `z1*`, `z2*`, and `zc*`.

**B. Differentiated architecture.**

```text
function 1 -> x
function 2 -> y.
```

**C. Fitness benchmark.** Show the one-axis compromise penalty and the extra architecture cost `K`:

```text
Delta_mod = [a b / (a + b)] (z1* - z2*)^2 - K.
```

**D. Optimum release.** Show `x*(y_high)` moving toward the Chapter-1 function-1 optimum `z1*`.

Primary message: an added trait dimension is valuable only insofar as it releases a quantified one-axis compromise.

## Figure 2 — Preferential functional loading

Display the trait-to-function response matrix:

```text
          function 1   function 2
x            r11          r12
y            r21          r22.
```

Illustrate three architectures:

1. integrated / pleiotropic: all entries large;
2. partially differentiated: diagonal entries dominate but cross-loading remains;
3. strongly modular: diagonal entries dominate and cross-loading is small.

Primary message: two traits are not automatically two modules.

## Figure 3 — Floral A x D outcome hierarchy

Show the four trait cells `W00`, `W10`, `W01`, `W11` and derive

```text
A0
A1
Delta_AD W.
```

Then display the nested outcome states:

```text
Level 1 interaction relief
Level 2 constraint release
Level 3 strict reversal.
```

Include the current Kessler aggregate-bound location: `A1` positive, `A0` close to and spanning zero, positive aggregate interaction sign, formal source/design uncertainty unresolved.

Primary message: interaction relief is a local dimensional-release test, not a full modularity claim.

## Figure 4 — 16-cell mechanism allocation and residual coupling

Show

```text
A x D x antagonist x pollinator.
```

Map the identifiable channel targets:

```text
rho_delta   antagonist relief
iota_delta  pollinator interference
m0_delta    pollinator-independent baseline term
U_delta     unallocated residual until independent cost assay.
```

Highlight the `A x D x G x P` four-way interaction as the residual functional-coupling diagnostic.

Primary message: mechanism allocation and degree of modularity are both properties of the same crossed design.

## Figure 5 — Real-world architecture evidence and claim ladder

Arrange current evidence on

```text
D0 distinct coordinates
D1 preferential functional loading
D2 functional release
D3 mechanism-resolved differentiation
D4 stable developmental/ecological modularity
D5 historical modularization.
```

Evidence anchors:

- component partitioning in floral scent bouquets;
- conditional cue gating;
- route separation / overlapped / bypassable defence architectures;
- Kessler 2008 A x D-like reproductive surface;
- current complete-system gap at D3;
- no direct historical shared-z -> x,y reconstruction.

Primary message: contemporary functional separation is real, but complete mechanism-resolved and historical modularization claims remain open.
