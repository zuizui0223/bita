# SCH–BITA figure symmetry v1

## Current paired figure logic

### SCH / Chapter 1

Current SCH main figure map:

```text
Fig. 1  theory: one shared coordinate, two pure functional objectives
Fig. 2  local z x consumer mechanism identification
Fig. 3  multi-level state-specific compromise geometry
Fig. 4  real-world evidence + claim ladder
Fig. 5  empirical handoff z_P*, z_G*, z_C* -> BITA
```

### BITA / Chapter 2

Current canonical BITA map:

```text
Fig. 1  shared architecture vs differentiated architecture
Fig. 2  architecture-cost / decoupling boundary
Fig. 3  robustness + partial-differentiation reality anchors
Fig. 4  multi-trait interaction -> mechanism identification
Fig. 5  fragmented identification frontier
```

## Symmetry assessment

```text
programme-level architecture:       PASS
theory continuation:               PASS
historical claim ceiling:           PASS
real-world grounding:               PASS
mechanism-identification logic:      PASS
explicit empirical release handoff: PARTIAL
```

The remaining asymmetry is not in the general theory. SCH Figure 5 now supplies an intervention-defined state-specific reference (`z_P*`) for Chapter 2, whereas the current BITA figures jump from theoretical architecture gain to the existing two-level `A×D` mechanism-identification framework.

The missing visual object is the **multi-level dimensional-release test**.

## Required BITA visual insertion

The next Figure 4 revision should begin with a small empirical-handoff panel before the existing mechanism-identification sequence:

```text
SCH receipt
z_P*, z_G*, z_C*
        |
        v
BITA multi-level x x y surface
x*(y0) ------> x*(y1)
     movement toward z_P*
        |
        v
R_state = |x*(y0)-z_P*| - |x*(y1)-z_P*|
        |
        +--> local A×D relief / release hierarchy
        |
        v
consumer-crossed mechanism allocation
```

Use a visibly separate optional lane for a pure reference:

```text
z_F1* only after SCH context-stable component-optimum gate PASS.
```

Do not draw `z_P* = z_F1*` as an identity.

## Figure 1 theory label correction

BITA Figure 1 may retain `theta1`, `theta2`, `L_S*`, `s` and `K`, but the caption should identify these as **theory-level function-specific optima / conflict load** rather than implying that current SCH experiments directly estimate pure `theta1`, `theta2`.

Recommended caption sentence:

> The shared-axis loss and function-specific optima in this panel are theory-level architecture quantities; the default SCH experiment instead exports intervention-defined state optima, and the corresponding empirical dimensional-release test is shown in Figure 4.

## Figure 4 role after revision

Figure 4 should distinguish three questions:

```text
1. DIMENSIONAL RELEASE
   did the optimum of x move toward the frozen SCH reference?

2. LOCAL CROSS-TRAIT EFFECT
   did y improve the local reproductive return to x?

3. MECHANISM ALLOCATION
   which consumer/pathway channels generated the observed change?
```

These are not interchangeable.

## Figure 5 remains unchanged

The 56/25 recurrence and 17-system fragmented frontier remain evidence about existing mechanism-identification coverage. They do not estimate how often dimensional release occurs and do not replace the new multi-level release gate.

## Paired reader takeaway

The visual sequence should make the dissertation logic readable without prose:

```text
SCH
one coordinate
-> causal compromise geometry
-> frozen function-facing reference

BITA
new coordinate
-> optimum release toward that reference
-> fitness gain / local cross-trait effect
-> mechanism allocation
-> historical modularization only with transition evidence.
```
