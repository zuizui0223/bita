# Pedicularis focal empirical execution gate v1

## Purpose

This file converts the current `Pedicularis rex` BITA focal bottleneck from a generic `empirical execution pending` label into an ordered, falsifiable execution gate.

The focal chain is not allowed to promote theory, literature anchors, or analyzer availability into a same-system causal differentiation claim.

## Current status

```text
T1-T5 theory                          READY under declared assumptions
C0 same-context SCH reference         BLOCKED: focal empirical receipt not yet present
C1 x x y dimensional release          BLOCKED: focal raw observations not yet present
C2 preferential functional loading    BLOCKED: prospective tolerance not yet frozen / focal execution absent
C3 registered fitness consequence     BLOCKED: focal execution absent
C4 mechanism allocation               LATER: only after C1-C3
C5 independent architecture cost K    LATER / optional for stronger architecture-level decomposition
```

Existing source-adjudicated Pedicularis evidence remains background/route evidence. It cannot satisfy C0-C3 because it does not provide the registered same-population, same-season, non-circular SCH -> BITA chain.

---

# Gate E0 — freeze the estimand contract before data entry

Required file:

```text
empirical/identification_design/PEDICULARIS_DIMENSIONAL_RELEASE_CONFIG_TEMPLATE_V1.json
```

must be copied to a run-specific frozen config and all `REQUIRED_BEFORE_USE` values replaced prospectively.

Required frozen quantities:

```text
min_dimensional_release
min_within_bita_fitness_gain
min_y_function2_gain
max_y_function1_penalty
min_y1_interior_bootstrap_fraction
```

The `max_y_function1_penalty` value is an equivalence/tolerance bound. A nonsignificant y -> pollination coefficient is not a substitute.

Pass condition:

```text
no placeholder values remain
status is no longer TEMPLATE_ONLY_DO_NOT_RUN...
threshold provenance is recorded before focal outcomes are analyzed
```

Failure condition:

```text
threshold chosen after seeing focal x x y outcomes
=> confirmatory focal chain invalid; exploratory-only analysis
```

---

# Gate E1 — obtain the non-circular SCH handoff

BITA requires a positive `Pedicularis rex` SCH receipt from the same population and season.

Required core fields:

```text
receipt_schema_version = SCH_CAUSAL_COMPROMISE_STATE_OPTIMA_V1
status = MODEL_SUPPORTED_CAUSAL_COMPROMISE_CANDIDATE
system_wrapper_schema_version = SCH_PEDICULARIS_FULL_SURFACE_WRAPPER_V2
G0 = SEED_PREDATOR_INDEPENDENTLY_EXCLUDED
G1 = SEED_PREDATOR_EXPOSED
water_y = HELD_FIXED_ACROSS_ALL_SCH_CELLS
readiness_reference.g_schema = SCH_PEDICULARIS_PREDATOR_METHOD_V3
predator_method_requirement includes POLLINATOR_ACCESS_PRESERVED
```

The Chapter-1 antagonist manipulation must be independent of the Chapter-3 water-defence y contrast. Reusing drained/protected water as both SCH G and BITA y is rejected as circular.

Pass condition:

```text
same population_id
same season_id
positive SCH causal-compromise candidate
method-qualified independent predator intervention
```

---

# Gate E2 — collect the registered x x y surface

Primary design:

```text
x = realized corolla exsertion
y0 = DRAINED water defence disabled
y1 = PROTECTED water defence active
>= 5 assigned x levels in both y states
```

Primary outcome:

```text
undamaged mature seed count per focal flower
```

Required companion outcomes:

```text
pollen grains
pollinator visits
undamaged + damaged initiated seeds
water depth
mechanical damage
realized exsertion
```

Raw schema:

```text
empirical/identification_design/PEDICULARIS_DIMENSIONAL_RELEASE_TEMPLATE_V1.csv
```

The current repository template is intentionally empty; a header-only file is not evidence.

---

# Gate E3 — preferential loading

The focal experiment must jointly support:

```text
y improves function 2
AND
y does not reduce function 1 beyond the frozen tolerance
```

For Pedicularis:

```text
function 1 = pollen receipt
function 2 = survival of initiated seeds
```

Published drainage results are prior/background evidence only. The focal chain must re-estimate the cross-effect prospectively.

Failure interpretation:

```text
y strongly protects seeds but materially impairs pollination
=> selective loading gate fails; do not call the state functionally differentiated at the registered ceiling
```

---

# Gate E4 — dimensional release

Fit the registered surfaces:

```text
W(x | y0)
W(x | y1)
```

with optima:

```text
x0* = optimum when water defence is disabled
x1* = optimum when water defence is active
```

Primary release estimand:

```text
R_state = |x0* - z_P*| - |x1* - z_P*|
```

where `z_P*` is inherited from the non-circular SCH state-specific reference unless a stronger independently identified pure-function reference was prospectively registered.

Pass requires the lower uncertainty bound to meet the frozen `min_dimensional_release` threshold and the released y1 surface to have adequate interior-optimum support.

A failure is scientifically informative:

```text
R_state <= threshold
=> a second defensive state exists but does not measurably release the shared-coordinate compromise in this context
```

---

# Gate E5 — registered fitness consequence

Require:

```text
best W(x | y1) - best W(x | y0)
>= frozen min_within_bita_fitness_gain
```

Report this as:

```text
within_bita_optimum_fitness_gain
```

not `Delta_mod`, `R-K`, or `sL-K`.

Architecture-level decomposition remains unidentified without an independent same-scale architecture-cost lane.

---

# Promotion rule

The focal `Pedicularis rex` result may be promoted to

```text
FUNCTIONAL_DIFFERENTIATION_OUTCOME_SUPPORTED
```

only if E0-E5 all pass.

The strongest resulting claim is:

> In the focal population and season, an independently manipulated second functional state preferentially carries antagonist protection, shifts the optimum of the previously conflicted exsertion coordinate toward the frozen SCH function-facing reference, and improves the registered reproductive outcome.

This still does not identify historical origin, genetic/developmental independence, invasion from rarity, coexistence, or architecture cost K.

---

# After the focal gate

Only after E0-E5 pass should the programme spend new effort on:

1. selective pollinator x antagonist mechanism allocation (C4),
2. structural-y promotion from acute water state to repeatable retention-performance trait,
3. an independent architecture-cost K lane if architecture-level crossing is claimed,
4. cross-system replication.

Until then, additional static theory is not the main BITA bottleneck.
