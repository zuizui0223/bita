# Chapter 2 submission scope v1 — archived promotion receipt

## Status — SUPERSEDED BY ACTIVE BITA MECHANISM-IDENTIFICATION SCOPE

This document records the earlier integrated architecture-plus-mechanism Chapter 2 promotion. It is retained for provenance only and is **not** the live submission contract.

The active submission scope is:

```text
docs/SUBMISSION_SCOPE.md
```

The active paper is:

```text
Trait interaction is not ecological mechanism
```

with canonical science source:

```text
manuscript/MANUSCRIPT_TRAIT_DIFFERENTIATION_V1.md
```

The active review-package builder and workflow are:

```text
scripts/build_bita_mechanism_candidate_sources.py
.github/workflows/build-bita-mechanism-review-package.yml
```

The former integrated package and its builders are retired:

```text
scripts/build_trait_differentiation_candidate_package_sources.py
scripts/build_ecology_review_package_sources.py
.github/workflows/build-trait-differentiation-candidate.yml
.github/workflows/build-ecology-submission-package.yml
```

The two legacy workflow filenames are preserved only as fail-closed routing guards. They must not rebuild or validate the superseded architecture-paper package.

## Historical package recorded here

The superseded paper combined:

```text
shared-axis conflict
-> recoverable loss under partial decoupling
-> architecture-cost threshold
-> mechanism identification after multiple axes exist
```

It included structural weak-dominance results, the quadratic `R=sL_S*` corollary, a registered nonquadratic robustness exercise, architecture-state anchors, and the BITA mechanism-identification layer. Those theory/provenance assets remain versioned in the repository, but architecture value is now owned by the companion SLK flagship rather than by the active BITA submission.

The previously validated package was:

```text
Main Document: 30 pages
Appendix S1:   38 pages
Main figures:   5
visual QA:      68/68 pages PASS
```

These counts describe the superseded integrated package only. They are not current BITA submission counts.

## Active BITA ownership

The active paper asks:

> When two traits interact on fitness, what does that interaction identify, which ecological mechanisms remain compatible with it, and what additional interventions are required to identify the mechanism?

Its main identification hierarchy is:

```text
Level 1  positive interaction relief
Level 2  functional constraint release
Level 3  strict reversal
        ↓
identified mechanism set
        ↓
partial identification
        ↓
selective crossed intervention
        ↓
consumer-channel allocation
        ↓
independent remaining-channel assay
```

The source-adjudicated empirical spine is:

```text
56 directional route records
25 independent biological clusters
17 high-information systems
0 systems closing full mechanism allocation + independent remaining-channel assay
```

This supports recurrent constituent biology and a fragmented identification frontier. It does not establish natural prevalence, historical trait splitting, or a universal mechanism.

## Literature-first empirical order

The active BITA order is:

```text
mathematical identification mechanism
-> observable route / identification signatures
-> source-adjudicated literature synthesis
-> compatible route-level quantitative lanes
-> residual mechanism-identification gap
-> focal crossed causal experiment last
```

A focal experiment is therefore a final identification upgrade, not the empirical entry point or a blocker for the current theory + literature-synthesis paper.

## Current claim ceiling

Route-level quantitative synthesis may quantify recurring constituent ecological channels. It does not allocate the total attraction-by-defence interaction among antagonist relief, pollinator interference, and a remaining joint channel. An unmeasured residual is not promoted to biological joint cost by subtraction.

Architecture-value quantities such as `R`, `K`, `s`, and `Phi` belong to SLK in the current publication architecture.

```text
STATUS = ARCHIVED_PROMOTION_RECEIPT
LIVE_SCOPE = docs/SUBMISSION_SCOPE.md
ACTIVE_PACKAGE = build-bita-mechanism-review-package.yml
```
