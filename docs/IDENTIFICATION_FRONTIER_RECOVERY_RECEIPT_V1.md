# Identification-frontier recovery receipt v1

## Scientific candidate

The current 16-system audit is recast from a binary `0/16 full identification` statement into a non-scalar identification frontier.

Fixed screened-set occupancy:

```text
direct A×D-like trait-factorial anchor: 1/16
consumer-factorial anchor:              1/16
randomized-context A×D anchor:          1/16
selective-D system anchor:              1/16
m0_delta characterized:                 0/16
independent kappa assay:                0/16
full allocation closure:                0/16
```

The first four modules are carried by four different studies. The candidate interpretation is therefore **design fragmentation**: relevant experimental information exists, but on orthogonal study backbones.

## New conditional bound

Kessler et al. 2008 has a published rounded probability-scale `Delta_AD` range of `+0.19 to +0.25`. Under the explicit same-scale auxiliary restriction `kappa_delta >= 0`, the accounting identity yields

```text
rho_delta - iota_delta >= +0.19
```

within those aggregate constraints. This is not a confidence bound because the factorial SE/CI is unrecovered, and it is not an empirical estimate of kappa.

## Decision gate before Main promotion

Promote this result only if:

1. the generated frontier matrix exactly reproduces the 16 source rows plus transparent derived labels;
2. the four anchor classes remain distinct studies;
3. no scalar ranking or literature-prevalence interpretation is introduced;
4. the Kessler numerical statement remains explicitly conditional and non-statistical;
5. normal repository CI and submission-scope remain green.

Until those gates pass, canonical Main text and figures remain unchanged.
